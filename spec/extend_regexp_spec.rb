require 'spec_helper'

describe ExtendRegexp do
  it 'has a version number' do
    expect(ExtendRegexp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
